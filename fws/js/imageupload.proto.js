//document.observe("dom:loaded", function() {
//});

function wsImageUpload(id) {
	alert('hi there');
	/*
	var e = $$('.zing-product-group');
	for (i = 0; i < e.length; i++) {
		v=e[i].up('li').down('ul');
		if (v.id!='group'+id) {
			Effect.BlindUp(v, {
				duration : 0.8
			});
		}
		else {
			Effect.BlindDown(v, {
				duration : 0.8
			});
		}
	};
	*/
}